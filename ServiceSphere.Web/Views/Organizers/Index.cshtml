@model IEnumerable<ServiceSphere.Domain.Entities.Organizer>

<h1>Organizer</h1>
<a asp-action="Create">Create Organizer</a>
<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var service in Model)
        {
            <tr>
                <td>@service.Name</td>
                <td>@service.Email</td>
                <td>
                    <a asp-action="Details" asp-route-id="@service.OrganizerId">Details</a>
                    <a asp-action="Edit" asp-route-id="@service.OrganizerId">Edit</a>
                    <a asp-action="Delete" asp-route-id="@service.OrganizerId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
